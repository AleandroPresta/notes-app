name: GitHub Pages

on:
  push:
    branches:
      - main # Set a branch name to trigger deployment
  pull_request:

jobs:
  deploy:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Install angular-cli-ghpages globally
      - name: Install angular-cli-ghpages
        run: npm install -g angular-cli-ghpages

      - name: Build
        run: npm run build -- --configuration production --base-href "https://aleandropresta.github.io/notes-app/"

      - name: Check build output
        run: ls -la dist/

      # Move the content of dist/notes-app/browser to dist/notes-app
      - name: Move build output
        run: mv ./dist/notes-app/browser/* ./dist/notes-app/

      # Remove the browser directory
      - name: Remove browser directory
        run: rm -rf ./dist/notes-app/browser

      # Print the content of dist/notes-app
      - name: Print build output
        run: ls -la dist/notes-app

      # Deploy using angular-cli-ghpages
      - name: Deploy to gh pages
        run: |
          npx angular-cli-ghpages --dir=dist/notes-app
        env:
          CI: true
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
